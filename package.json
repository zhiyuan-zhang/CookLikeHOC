{
  "name": "cooklikehoc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prebuild:indexes": "node ./.vitepress/scripts/generate-indexes.mjs",
    "docs:dev": "node generate-dish-json.js && npm run prebuild:indexes && vitepress dev .",
    "docs:build": "node generate-dish-json.js && npm run prebuild:indexes && vitepress build .",
    "docs:preview": "node generate-dish-json.js && vitepress preview .",
    "build": "npm run docs:build",
    "start": "npm run docs:dev"
  },
  "devDependencies": {
    "vitepress": "^1.3.4"
  },
  "dependencies": {
    "vue": "^3.5.21"
  }
}
